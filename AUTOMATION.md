# SRMS Lab 자동화

## 새 컴퓨터 셋업

1. 프로젝트 폴더를 압축 해제하거나 복사
2. `실행.bat` 더블클릭

Chrome이 이미 설치돼 있어야 합니다. Node.js는 별도 설치 불필요.

로그인 세션은 컴퓨터마다 새로 만들어집니다. 첫 실행 시 SRMS에 직접 로그인하면 이후에는 자동으로 유지됩니다.

로그인 세션과 수집 데이터는 `%LOCALAPPDATA%\TrinityLabSummary`에 저장됩니다.

---

## 사용 방법

`실행.bat`을 더블클릭하면 모드를 선택할 수 있습니다.

### [1] 개인 조회

환자 이름과 날짜를 터미널에서 입력합니다.

```
환자 이름 (여러 명은 쉼표로 구분): 홍길동, 김영희
조회 시작일 [20260410]: (Enter — 기본값 사용)
조회 종료일 [20260609]: (Enter — 기본값 사용)
```

Chrome이 열리면 SRMS 로그인 후 검사결과 목록 화면으로 이동합니다.
목록이 뜨면 터미널에서 Enter를 누르면 자동 수집이 시작됩니다.

### [2] 병동 일괄 처리

`ward-patients.txt`에 한 줄에 한 명씩 환자 이름을 입력합니다.

```text
조경순4
김영희
박철수2
```

이름 뒤 숫자가 있으면 그대로 입력합니다.

### 재처리

분류 규칙을 수정한 뒤 다시 SRMS에 접속하지 않고 마지막 수집 결과를 재처리합니다.

```
node\node.exe src\reprocess.mjs
```

---

## 결과 확인

수집이 끝나면 브라우저가 자동으로 열리고 결과가 바로 표시됩니다.
환자 선택 → 내용 확인 및 수정 → 전체 결과 복사.

결과 파일은 `%LOCALAPPDATA%\TrinityLabSummary\output\`에 저장됩니다.

---

## 미분류 항목이 나오면

`미분류`가 나오면 검사명이나 검체명을 확인해서 `src/classify.mjs` 상단의 분류 규칙을 추가합니다.

---

## 주의

- `%LOCALAPPDATA%\TrinityLabSummary`에는 로그인 세션과 환자 정보가 포함됩니다.
- 외부 공유나 클라우드 동기화를 피하세요.
